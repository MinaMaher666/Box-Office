<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/sorted_by_now_playing"
        android:orderInCategory="100"
        android:title="@string/sorted_by_now_playing_lable"
        app:showAsAction="never" />

    <item
        android:id="@+id/sorted_by_popular"
        android:orderInCategory="101"
        android:title="@string/sorted_by_popular_lable"
        app:showAsAction="never" />

    <item
        android:id="@+id/sorted_by_top_rated"
        android:orderInCategory="102"
        android:title="@string/sorted_by_top_rated_lable"
        app:showAsAction="never" />
</menu>